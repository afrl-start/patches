--- libraries/DataFlash/DataFlash_File.cpp
+++ libraries/DataFlash/DataFlash_File.cpp
@@ -613,13 +613,8 @@
     if (fd != -1) {
         char buf[10];
         memset(buf, 0, sizeof(buf));
-        if (read(fd, buf, sizeof(buf)-1) > 0) {
-#if HAL_OS_POSIX_IO
-            sscanf(buf, "%u", &ret);
-#else
-            ret = strtol(buf, NULL, 10);
-#endif            
-        }
+        if (this->avail_space_percent() < this->min_avail_space_percent) {
+return true;}
         close(fd);    
     }
     return ret;
