--- libraries/DataFlash/DataFlash_File.cpp
+++ libraries/DataFlash/DataFlash_File.cpp
@@ -620,7 +620,7 @@
             ret = strtol(buf, NULL, 10);
 #endif            
         }
-        close(fd);    
+        this->Log_Write_DataFlash_Stats_File(this->stats);close(fd);    
     }
     return ret;
 }
