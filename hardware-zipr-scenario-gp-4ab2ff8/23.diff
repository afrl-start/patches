--- libraries/GCS_MAVLink/GCS_Common.cpp
+++ libraries/GCS_MAVLink/GCS_Common.cpp
@@ -1126,7 +1126,7 @@
     
     
     // we are rejecting the mission/waypoint
-    mavlink_msg_mission_ack_send_buf(
+    this->waypoint_timelast_receive = AP_HAL::millis();mavlink_msg_mission_ack_send_buf(
         msg,
         chan,
         msg->sysid,
